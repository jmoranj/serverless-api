# Local development:
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=password
DB_NAME=requests_db

# Deploy AWS (no shell ou CI, não no .env com host local):
VPC_ID=vpc-...
DB_USER=admin
DB_PASSWORD=password
DB_NAME=requests_db
